[package]
name = "autoharness-store-sqlite"
description = "SQLite durable session storage adapter for AutoHarness"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
autoharness-domain = { path = "../autoharness-domain" }
autoharness-memory = { path = "../autoharness-memory" }
autoharness-store = { path = "../autoharness-store" }
rusqlite = { version = "0.40.1", features = ["bundled"] }
serde = "1.0"
serde_json = "1.0"
sha2 = "0.11.0"

[dev-dependencies]
tempfile = "3.27.0"

[lints]
workspace = true
